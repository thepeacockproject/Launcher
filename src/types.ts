export const enum GameVersion {
    H1,
    H2,
    H3,
}

export const enum GameProvider {
    Steam,
    Epic,
    GOG,
    /**
     * Not used until we add gamepass support.
     *
     * @deprecated
     */
    Scarlett,
}
